============
Installation
============

At the command line::

    $ easy_install gm-auth-sdk

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gm-auth-sdk
    $ pip install gm-auth-sdk
